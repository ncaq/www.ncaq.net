{
  "scripts": {
    "default.css": "sass site/default.scss",
    "fix": "yarn lint --write",
    "lint": "shopt -s nullglob && prettier --ignore-path .gitignore --check **/*.{js,jsx,ts,tsx,json,html,css,less,sass,scss,yml,yaml}"
  },
  "devDependencies": {
    "bootstrap": "^4.6.0",
    "prettier": "^2.8.4",
    "sass": "^1.58.3"
  }
}
