{
  "scripts": {
    "default.css": "sass site/default.scss",
    "fix": "yarn lint --write",
    "lint": "prettier --ignore-path .gitignore --ignore-unknown --check '**/*.{js,jsx,ts,tsx,json,html,css,less,sass,scss,yml,yaml}'"
  },
  "devDependencies": {
    "prettier": "^2.8.4",
    "sass": "^1.58.3",
    "wrangler": "^2.12.0"
  },
  "dependencies": {
    "bootstrap": "^4.6.0"
  }
}
